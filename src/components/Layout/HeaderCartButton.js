import CartIcon from "../Cart/Cart";

const HeaderCartButton = (props) => {
  return (
    <button>
      <span>
        <CartIcon />
      </span>
      <span></span>
      <span></span>
    </button>
  );
};

export default HeaderCartButton;
